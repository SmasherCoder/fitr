import { LOAD_WORKOUTS,
  LOAD_FRIENDS,
  ADD_WORKOUT,
  UPDATE_WORKOUT,
  REMOVE_WORKOUT,
  ADD_FRIEND,
  REMOVE_FRIEND
  }  from "./actions";
import { useReducer } from 'react';

export const reducer = (state, action) => {
  switch (action.type)
}
